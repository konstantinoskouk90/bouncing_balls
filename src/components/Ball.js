import React, { Component } from 'react';

export default class Ball extends Component {
    
    constructor(x, y) {
        
        super();
        
        this.x = x;
        this.y = y;
    }
}